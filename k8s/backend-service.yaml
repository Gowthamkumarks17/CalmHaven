apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
  # type: ClusterIP       # it will access the app from cluster or pods.
    type: LoadBalancer    # to access the app from internet
  ####just working .
